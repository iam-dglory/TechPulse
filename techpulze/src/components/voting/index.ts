/**
 * Voting Components
 *
 * Premium voting interface for TechPulze scoring system
 * Includes full 5-dimensional voting UI and compact display variants
 */

export { VotingInterface } from './VotingInterface'
export { VotingCard, VotingStats } from './VotingCard'

// Re-export types for convenience
export type { VoteType } from '@/types/database'
